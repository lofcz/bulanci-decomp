# CDSChain_LoadConfigFromRegistry_finally @ 0x0040a650

## Status

**VERIFIED** — MSVC `__finally` epilogue tail for `CDSChain_LoadConfigFromRegistry` @ `0x0040a440`. Runs on **normal exit** (JMP) and on **exception unwind** (catch resume). Not dead code.

## Size proof

| Claim | Address | Evidence |
|-------|---------|----------|
| Finally epilogue span `0x21` | `CDSChain_LoadConfigFromRegistry_finally@0x0040a650` | Ghidra body `0x0040a650`–`0x0040a670`; last insn `RET` @ `0x0040a670` |
| EBX-reload prefix `0x3` (unwind-only entry) | `CDSChain_LoadConfigFromRegistry_finally_resume@0x0040a64d` | `MOV EBX,[EBP+0x4c]` then fall-through to `@0x0040a650` |
| Catch handler span `0x1e` | `CDSChain_LoadConfigFromRegistry_catch@0x0040a62f` | Ends `RET` @ `0x0040a64c`; continuation `EAX=0x0040a64d` |
| Parent try body ends before finally | `CDSChain_LoadConfigFromRegistry@0x0040a440` | Ghidra body `0x0040a440`–`0x0040a62e`; sole tail `JMP 0x0040a650` @ `0x0040a62d` |

## Layout

**SKIP** — not a struct; shared stack frame with parent `CDSChain_LoadConfigFromRegistry`.

### Parent EBP frame offsets (proven)

| EBP offset | Role | Evidence |
|------------|------|----------|
| `+0x4c` | `CDSChain_full *this` (saved in EBX) | `MOV [EBP+0x4c],EBX` @ `0x0040a472` after `MOV EBX,ECX` @ `0x0040a470` |
| `-0xc` | Saved `ExceptionList` / FS:[0] node | `LEA EAX,[EBP-0xc]` + `MOV FS:[0],EAX` @ `0x0040a464`–`0x0040a467`; restored @ `0x0040a65c`–`0x0040a65f` |
| `-0x4` | SEH try-state (`0`→`1`→`2`→`3`→`4`→`0xffffffff`) | `MOV [EBP-0x4],imm` at `@0x0040a486,0x0040a4b2,0x0040a4c9,0x0040a4e9,0x0040a5ec,0x0040a605,0x0040a618,0x0040a626` |
| `+0x48` | Pointer dtor’d in catch (vtable+8) | Catch @ `0x0040a62f`: `MOV ECX,[EBP+0x48]` → indirect call `[ECX]+8` |
| `+0x50` | `CDSRegKey` local | Unwind helper `@0x00475310`: `LEA ECX,[EBP+0x50]` |
| `+0x54` | `CDSString` stack pointer | `MOV [EBP+0x54],ESP` @ `0x0040a480`; unwind `@0x00475328` |

### `[EBX+0x49]` in finally

| Offset | Field | Evidence |
|--------|-------|----------|
| `+0x49` | `dwPalette0` (`uint`) | Finally @ `0x0040a650`: `MOV ECX,[EBX+0x49]` → `CDSAudio_SetPanPreview`; registry read @ parent `0x0040a598`–`0x0040a5a1` |

## SEH structure proof

### Registration (@ parent prologue)

```
0040a448  PUSH -0x1
0040a44a  PUSH 0x475330          ; LAB_00475330
0040a44f  MOV  EAX, FS:[0]
0040a455  PUSH EAX
...
0040a464  LEA  EAX, [EBP-0xc]
0040a467  MOV  FS:[0], EAX
```

### Handler `LAB_00475330` @ `0x00475330`

- Stack-cookie check (`XOR ECX,EAX`; `CALL 0x00447bd5`)
- `MOV EAX, 0x004a69dc` (FuncInfo)
- `JMP 0x00447897` (`___CxxFrameHandler3`)

### FuncInfo / try map (raw `.rdata`)

| Address | Content | Meaning |
|---------|---------|---------|
| `0x004a69c4` | `0x0040a62f` | Catch handler RVA (xref DATA) |
| `0x004a69c8` | tryLow=`0`, tryHigh=`4`, catchHigh=`5`, nCatches=`1` | TryBlockMapEntry |
| `0x004a69b8` | handler=`0x0040a62f`, length=`0x48` | CatchBlockMapEntry |
| `0x004a6a00` | UnwindMap | State→dtor thunks @ `0x00475310/18/20/28/40` |

### Unwind dtor thunks (@ `0x00475310`)

| State | Thunk | Target dtor |
|-------|-------|-------------|
| `0` | `@0x00475310` | `[EBP+0x50]` → `CDSRegKey` close |
| `1` | `@0x00475318` | `[EBP-0x3c]` → `CDSEasyMemStream` |
| `2` | `@0x00475320` | `[EBP+0x0]` → `CDSGZipStream` |
| `0` | `@0x00475328` | `[EBP+0x54]` → `CDSString` release |

## Control-flow paths to `@0x0040a650`

| Path | Entry | Evidence |
|------|-------|----------|
| **Normal success** | `JMP 0x0040a650` @ `0x0040a62d` | After `[EBP-0x4]=0xffffffff` @ `0x0040a626`; sole xref **to** `@0x0040a650` |
| **Exception catch → finally** | `@0x0040a64d` → `@0x0040a650` | Catch `@0x0040a62f`: `CDSGZipStream*` @ `[EBP+0x48]` → vtable `+0x8` release, `CBulanci_InstallFactoryDefaults(this)`, `bField_78=1`, resume `@0x0040a64d` |
| **Not reachable** | direct `CALL` | `get_xrefs_to(0x0040a650)` → only `0x0040a62d` (JMP, not CALL) |

## Finally disassembly

```
0040a64d  MOV EBX, [EBP+0x4c]     ; unwind-only: reload this
0040a650  MOV ECX, [EBX+0x49]     ; dwPalette0
0040a653  PUSH ECX
0040a654  CALL CDSAudio_SetPanPreview
0040a65c  MOV ECX, [EBP-0xc]      ; saved ExceptionList
0040a65f  MOV FS:[0], ECX
0040a666  POP ECX                 ; stack cookie
0040a667  POP EDI
0040a668  POP ESI
0040a669  POP EBX
0040a66a  ADD EBP, 0x5c
0040a66d  MOV ESP, EBP
0040a66f  POP EBP
0040a670  RET
```

## Ghidra apply

- Renamed `FUN_0040a650` → **`CDSChain_LoadConfigFromRegistry_finally`**
- Prototype: `void __cdecl CDSChain_LoadConfigFromRegistry_finally(void)`
- Labels: `CDSChain_LoadConfigFromRegistry_catch` @ `0x0040a62f`, `CDSChain_LoadConfigFromRegistry_finally_resume` @ `0x0040a64d`
- Decompiler PRE_COMMENT @ `0x0040a650` (EBX/EBP frame semantics)
- `save_program bulanci.exe`

## Decompile vs inline recommendation

**Keep as separate Ghidra function; inline conceptually in C++ reimplementation.**

Ghidra must retain `@0x0040a650` as its own function because it is the shared merge point for parent `JMP` and SEH continuation `@0x0040a64d`. For readable C++, treat as a `finally` block at the end of `CDSChain_LoadConfigFromRegistry` (see snippet below).

## UNK

- *(R5 worker 43 closed)* ~~Symbolic name of IDSChained vtable slot `+0x10`~~ → **`CDSChain_Append@0x0042fb70`** (`g_pCDSChain_vftable_IDSChained@0x0047f6b8` slot **[4]**). Disasm @ `0x0040a5ce`–`0x0040a5df`: `MOV EAX,[EBX+0x68]`; `[EAX+0x10]`; `LEA ECX,[EBX+0x68]`; `PUSH [EBP+0xc]` (gzip read face); `CALL EAX`. See [`CDSChain_LoadConfigFromRegistry.md`](CDSChain_LoadConfigFromRegistry.md).

## Resolved (catch `[EBP+0x48]`)

**Type:** `CDSGZipStream *pGzipForEh` — points at the stack `CDSGZipStream` at `[EBP+0]` (`sizeof == 0x48`). Catch @ `0x0040a62f` calls **`[vtable+0x8]`** on that object (`FUN_00401660`, IDSReferenced release/refcount), **not** the direct `CDSGZipStream_dtor@0x004098a0` used on the success path. Full proof: [`CDSChain_LoadConfigFromRegistry.md`](CDSChain_LoadConfigFromRegistry.md) § “Catch”.
